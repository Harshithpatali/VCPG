services:
  - type: web
    name: vcpg-backend
    runtime: python
    plan: free

    buildCommand: |
      pip install uv
      uv sync

    startCommand: |
      uvicorn backend.main:app --host 0.0.0.0 --port $PORT